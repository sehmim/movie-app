<%= simple_form_for @play do |f| %>
    <%# <%= select_tag(:category_id, options_for_select(@catagories)) %>
    <%= f.file_field :play_img %>
    <%= f.input :title, label: "Movie Name"%>
    <%= f.input :description, label: "Description" %>
    <%= f.input :director, label: "Director" %>
    <%= f.button :submit, label: "Add" %>
<% end %>